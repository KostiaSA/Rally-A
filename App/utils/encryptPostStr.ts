// import crypto = require("crypto-js");
// import {appState} from "../AppState";
//
// export function encryptPostStr(message: string): string {
//     return crypto.AES.encrypt(message, appState.encryptKey).toString();
// }
